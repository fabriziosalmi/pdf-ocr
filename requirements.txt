# Core dependencies
Flask==2.3.3
Werkzeug==2.3.7
Pillow>=9.4.0
pytesseract>=0.3.10
pdf2image>=1.16.3
python-docx>=0.8.11

# Tesseract wrapper (always works)
pytesseract>=0.3.10

# EasyOCR (optional)
easyocr>=1.7.0

# PaddleOCR (optional - may have compatibility issues)
# paddlepaddle==2.4.2  # CPU version
# paddleocr==2.6.0.3

# Kraken (optional - requires specific protobuf version)
# kraken==4.3.0
# protobuf<=3.20.0  # Required for Kraken compatibility

# PyOCR (optional)
# pyocr>=0.8.3

# Image Processing
opencv-python>=4.5.0
scikit-image>=0.18.0
scipy>=1.7.0

# Security and Performance
gunicorn>=20.1.0  # WSGI production server
python-dotenv>=0.19.0  # Environment variable management

# Note: You also need to install poppler externally:
# On macOS: brew install poppler
# On Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases
# On Linux: apt-get install poppler-utils
# 
# For Tesseract OCR languages:
# On macOS: brew install tesseract-lang
# On Linux: apt-get install tesseract-ocr-[lang] (replace [lang] with language code) or tesseract-ocr-all