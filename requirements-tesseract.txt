# Tesseract is the default engine and needs nothing beyond the core
# requirements — pytesseract is already pinned there. This file is kept only
# so `install_dependencies.py --engine tesseract` and older docs still resolve.
#
# You still need the Tesseract binary itself:
#   macOS  brew install tesseract tesseract-lang
#   Debian apt-get install tesseract-ocr tesseract-ocr-<lang>
-r requirements.txt
