# EasyOCR engine — additive on top of the pinned core requirements.
# Install with: pip install -r requirements-easyocr.txt
#
# This file used to restate Flask/Werkzeug/Pillow with floating `>=` bounds,
# which silently overrode the pins in requirements.txt.
-r requirements.txt

easyocr>=1.7,<2.0
# PyTorch: if the default wheel is wrong for your platform/CUDA, install it
# first from https://pytorch.org/get-started/locally/
torch>=2.13.0
torchvision>=0.28.0
opencv-python-headless>=5.0.0.93   # headless: no GUI libs needed on a server
