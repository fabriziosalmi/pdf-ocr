# PaddleOCR engine — additive on top of the pinned core requirements.
# Install with: pip install -r requirements-paddleocr.txt
#
# Pinned to the 2.x line: 3.x removed use_angle_cls/cls/show_log and renamed
# .ocr() to .predict(), which app.py's dispatch does not target.
-r requirements.txt

paddleocr>=2.6,<3.0
paddlepaddle>=2.5,<3.0
opencv-python-headless>=5.0.0.93
